# Pyramid frontend

Next.js (App Router) UI for the Pyramid task management assessment.

```bash
cp .env.example .env.local
npm install
npm run dev
```

Runs on `http://localhost:3001`. See the root [README](../README.md) for API setup and production deploy.
