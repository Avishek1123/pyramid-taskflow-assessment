# Pyramid API

NestJS backend for the Pyramid task management assessment.

```bash
cp .env.example .env
npm install
npx prisma db push
npm run start:dev
```

Runs on `http://localhost:4000/api`. See the root [README](../README.md) for full setup and deploy steps.
